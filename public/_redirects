# Netlify redirects for SPA (Single Page Application)
# This ensures all routes are handled by the client-side router

# Redirect all routes to index.html for client-side routing
/*    /index.html   200

# API routes (if any) should be handled before the catch-all
# /api/*  /api/:splat  200

# Static assets should be served directly
# These are handled automatically by most static hosting services
